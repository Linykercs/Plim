# Sons de notificação

Adicione o arquivo `plim.wav` nesta pasta.

O som será tocado nas notificações de alarme de xixi.
Formatos aceitos: `.wav` (recomendado), `.mp3`, `.aiff`.

Depois de adicionar o arquivo, rode o build novamente com `eas build`.
